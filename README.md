# Project Setup

## Database (Postgres + PostGIS)

### Prerequisites
Install Docker Desktop and make sure it is running.

---

### Start the database

From the project root:

```bash
docker compose up -d
